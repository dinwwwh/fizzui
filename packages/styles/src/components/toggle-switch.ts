import { compose } from '@fizzui/compose'
import { toggleSwitchRoot, toggleSwitchThumb } from './toggle-switch.css'

export const toggleSwitch = compose({
  root: toggleSwitchRoot,
  thumb: toggleSwitchThumb,
})
